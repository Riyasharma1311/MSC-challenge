#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    
    int n;
    cin>>n;
    
    vector<int>V;
    
    for(int i=0; i<n; i++){
        int x;
        cin>>x;
        V.push_back(x);
    }
    sort(V.begin(), V.end());
    
     for(int i=0; i<n; i++){
        
        cout<< V[i]<<" ";
    }
    return 0;
}
